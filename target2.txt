                if (classDaySched) {
                  const hasOtherSameCourse = classDaySched.some((s, sIdx) => 
                     s !== null && 
                     s.assignmentId !== confId &&
                     s.courseId === confAssign.courseId
                  );
                  if (hasOtherSameCourse && !globalAllowSameDaySameCourse && (!options?.priorityAssignmentIds || !options.priorityAssignmentIds.includes(confId))) physicallyImpossible = true;
                }
              }
              if (physicallyImpossible) continue;

